# Playwright Tests (.NET)

Place Playwright for .NET tests in:

- `CalendarTasking.PlaywrightTests`

Target coverage:

1. End-to-end UI flows through `client/` served by API.
2. API tests through HTTP calls from Playwright test suite.
